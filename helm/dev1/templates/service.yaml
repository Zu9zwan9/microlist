apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev1.fullname" . }}
  labels:
    {{- include "dev1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev1.selectorLabels" . | nindent 4 }}
