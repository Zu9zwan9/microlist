apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dev1.fullname" . }}-test-connection"
  labels:
    {{- include "dev1.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dev1.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
